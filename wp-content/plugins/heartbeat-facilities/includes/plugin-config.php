<?php

/*
 * Site Prefix
 */
if( !defined( 'HEARTBEAT_SITE_PREFIX' ) ) {
	define( 'HEARTBEAT_SITE_PREFIX', 'ad_' );
}

/*
 * Plugin Directory
 */
if( !defined( 'HEARTBEAT_FACILITIES_PLUGIN_DIR' ) ) {
	define( 'HEARTBEAT_FACILITIES_PLUGIN_DIR', dirname( __FILE__ ) . '/..' );
}

/*
 * Plugin Directory
 */
if( !defined( 'HEARTBEAT_ICON_SLUG' ) ) {
	define( 'HEARTBEAT_ICON_SLUG', 'hb-icons' );
}

